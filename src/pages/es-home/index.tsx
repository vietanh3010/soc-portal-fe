import { AppConfig } from "@/common/AppConfig";
import { memo, useEffect } from "react"


  
const EsHome = (): JSX.Element => {
    

    return (
        <div>dcmm es home</div>
    )
}

export default memo(EsHome)